export type FlightPreview = {
  id: string;
  flightNumber: string;
  dateLabel: string;
  origin: string;
  destination: string;
  originCode: string;
  destinationCode: string;
  departureTime: string;
  arrivalTime: string;
  status: string;
  duration: string;
  aircraft: string;
};
